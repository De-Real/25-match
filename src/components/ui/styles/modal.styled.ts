import { styled } from "styled-components";

export const StyledModal = styled.div`
	display: flex;
	flex-direction: column;
	align-items: center;
	row-gap: 40px;
	column-gap: 10px;
	& h2 {
		font-size: 24px;
		font-weight: 700;
	}

	flex-wrap: wrap;
`;
